# Usage

Make sure RStudio is installed.

Open it, then in the Console, do


```
setwd( "/Users/maco/Desktop/Extracurricular/LiveNote/LifeTracker/")
shiny::runApp("Manualnputs_withPlot.R")
```

Check current directory: getwd()
